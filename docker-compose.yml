version: "3"
services:
  githubarchive:
    build:
      context: githubArchive/
      dockerfile: Dockerfile
  datastorage:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./dbs/mongo/db:/data/db
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 15672:15672
    volumes:
      - ./rabbitManagement/rabbitmq.config:/etc/rabbitmq/rabbitmq.config:ro
      - ./rabbitManagement/definitions.json:/etc/rabbitmq/definitions.json:ro
  search:
    image: elasticsearch
    ports:
      - 9200:9200
    volumes:
      - ./dbs/esdata1:/usr/share/elasticsearch/data
    tty: true
  kibana:
    image: kibana
    ports:
      - 5601:5601
  recommandation:
    build:
      context: .
      dockerfile: ./recommandation/Dockerfile
    ports:
      - 7474:7474
      - 7687:7687
    volumes:
      - ./dbs/neo4j/data:/data
      - ./dbs/neo4j/plugins:/plugins
    tty: true
  prediction:
    build:
      context: .
      dockerfile: ./prediction/Dockerfile
    ports:
      - 8889:8889
      - 4040:4040
    volumes:
      - ./prediction/data:/home/jovyan/data
    tty: true
